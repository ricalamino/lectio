{
  "name": "lectio",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted, model-agnostic capture-first knowledge app",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "2.1.3",
    "typescript": "5.6.2",
    "@types/node": "20.16.10"
  }
}
